import React from 'react';
import { StyledLandingSection } from './LandingSection.js';

export const LandingSection = ({children}) => {
  return (
    <StyledLandingSection>{children}</StyledLandingSection>
  )
}

