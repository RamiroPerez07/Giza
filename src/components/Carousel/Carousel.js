import styled from "styled-components";
import { Box } from "@chakra-ui/react";

export const StyledContainer = styled(Box)`
  display: grid;
  place-items: center;
  padding: 10px;
  width: 100%;
  max-width: 600px;
`; 

export const ArrowBtn = styled(Text)`

`;

